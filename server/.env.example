PORT=4000
CORS_ORIGINS=http://localhost:8081,http://localhost:19006
# node -e "console.log(require('crypto').randomBytes(48).toString('base64url'))"
JWT_SECRET=
# node -e "console.log(require('crypto').randomBytes(32).toString('base64'))"  (EXACTLY 32 bytes)
SECRETS_MASTER_KEY=
SEED_ADMIN_EMAIL=islamov.denis@gmail.com
# empty -> a strong password is generated and printed once on first start
SEED_ADMIN_PASSWORD=
DATA_FILE=./data/store.json
